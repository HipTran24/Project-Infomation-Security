spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 10
      auto-commit: true
      pool-name: HikariCP
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
  mail:
    host: smtp.gmail.com
    port: 587
    username: tranthanhhiep8125@gmail.com  # thay bằng email của bạn
    password: vtwi fmor asnr wale    # App Password (không phải mật khẩu gmail thường)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

app:
  verify:
    base-url: http://localhost:8080/mydb/khachhang/verify   # đường dẫn backend hoặc frontend để verify
    token-expiration-hours: 1                              # token hết hạn sau 24h
    max-unverified-days: 1


server:
  port: 8080
  servlet:
    context-path: /mywed